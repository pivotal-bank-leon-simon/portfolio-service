spring:
  profiles.active: local,localmysql
  application:
     name: portfolio-service
  main:
    allow-bean-definition-overriding: true
info:
  build:
    group: ${group}
    name: ${name}
    description: ${description}
    version: ${version}